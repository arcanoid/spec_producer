require 'spec_helper'

describe SpecProducer do
  it 'has a version number' do
    expect(SpecProducer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
